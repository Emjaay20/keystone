# Slice 1 — Identity

Goal: a person can register, sign in, create an organization, and the API
knows which user and which tenant the request belongs to.

This slice does not yet decide product permissions. That is Slice 2.
